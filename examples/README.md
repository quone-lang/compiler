# Compiler Smoke Examples

These examples are intentionally small and should compile during release
validation. Larger user-facing examples live in the top-level `examples`
repository.

```sh
quonec check examples/hello.Q
quonec compile examples/hello.Q
```

